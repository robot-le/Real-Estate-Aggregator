container_commands:
  01_migrate:
    command: "source /var/app/venv/*/bin/activate && python manage.py migrate"
    leader_only: true
  # 02_scrape:
  #   command: "source /opt/elasticbeanstalk/deployment/env && source /var/app/venv/*/bin/activate && python manage.py crawl-all"
option_settings:
  aws:elasticbeanstalk:application:environment:
    DJANGO_SETTINGS_MODULE: aggregator.settings
